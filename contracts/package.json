{
  "name": "slots-parking-contracts",
  "version": "1.0.0",
  "description": "SLOTS Parking Booking Smart Contracts on Polygon",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network polygon",
    "deploy:testnet": "hardhat run scripts/deploy.js --network polygonAmoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0"
  }
}